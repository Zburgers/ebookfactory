# Prompt scaffold

Implement versioned orchestrator, nonfiction, fiction and review profiles. The orchestrator dynamically writes task instructions using approved brief/revisions. Profiles enforce output schemas, evidence, bounded delegation and quality, without fixed named personalities. Store prompt/profile version on each task attempt. No prompt may claim authority to bypass tool scope or approve its own brief.
