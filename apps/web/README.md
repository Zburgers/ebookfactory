# Dashboard scaffold

Planned React application using maintained Ant Design/Markdown/editor components. Pages: Projects, Studio, Usage, Settings. Studio combines chat, brief/review decisions, task events, chapters and exports. Reuse MuMu interaction ideas. No bespoke chat component library. Generate typed API clients from shared contracts; SSE reconnect uses persisted event IDs.
