# Pi worker scaffold

Planned TypeScript trusted supervisor with explicit Pi resources. `src/{supervisor,pi,providers,tools,model-gateway,sandbox,production,usage,codex-art}.ts` are intended module boundaries, not mandatory abstractions. Pin installed SDK after P00. Host chat has scoped tools only; durable approved production executes in rootless containers. Use private API job/tool endpoints; do not duplicate SQL ownership.
