# Goal prompt for GPT 5.6 Luna high/xhigh

Work in `/home/naki/Desktop/itsthatnewshit/isthisreal/ebook-factory-v2`.

Your goal is to implement and operate the personal Ebook Factory defined in this directory, including its dashboard, Pi orchestrator, PostgreSQL durable jobs, isolated production runs, usage tracking, Codex subscription art tool, Telegram and validated publishing exports. The owner wants a working product, minimal interruptions, and reuse of existing UI/domain patterns. This is a build assignment, not another planning exercise.

First read AGENTS.md and every document linked from README.md, especially the build packets, contracts, publishing rules and acceptance rubric. The current tree is a design scaffold, not a working application. Verify runtime facts afresh. Preserve the sibling legacy Ebookmaker and MuMuAINovel reference trees.

Use GPT 5.6 Luna high/xhigh as lead. Spawn GPT 5.6 Luna low implementers for bounded disjoint packets and separate GPT 5.6 Luna high critics. Verify the tool actually supports those model/effort settings; never claim a model or independent agent that was not used. Escalate difficult state/recovery work rather than relying on low-effort guesses. Keep concurrent writers away from shared contracts, migrations and ledger.

Execute P00–P10 in dependency order. Finish safe independent work while inputs are pending. Exact PostgreSQL server/database/role still needs owner confirmation before provisioning/migrations. Telegram token and allowed chat/sender IDs must be provided locally. Reuse the global Pi catalog/auth through trusted APIs as authorized; do not inherit unrelated global extensions or mount home into production containers. No OpenAI Agents SDK or paid image API substitution. Establish the actual unattended Codex subscription image route early and record any blocker precisely.

The user journey is: brainstorm with one main orchestrator -> approve a specific brief -> autonomous bounded research/planning/drafting/review -> review and revise draft/art -> download a validated package. Subagents never interrupt the owner directly. Production survives closed tabs and process restarts; provider limits may pause safely with a visible reason. General ebooks use one revisioned document core with fiction/nonfiction profiles.

Use an implementer/critic loop for every integrated packet: implement, run meaningful checks, obtain independent critique, fix findings, rerun affected checks, commit locally and update the status/scorecard. Append only one or two concise ledger lines per completed assignment using actual agent IDs, model/effort, delivered revision/evidence, outcome, confidence and why. Lead serializes writes to docs/AGENT_LEDGER.md. Never rewrite prior rows or include chain-of-thought/secrets.

Completion requires strictly >80/100 (at least 81) on docs/ACCEPTANCE.md AND every hard gate. Aim to complete the whole requested scope; reaching 81 is not a reason to abandon remaining safe work. Confidence is not completion. Use real provider, container, browser, Telegram, manuscript and export evidence. Missing quota fields must be unknown, not fabricated; API-equivalent cost must not be labelled actual subscription billing. Fake artifacts, canned text and mock-only checks cannot qualify.

Persist toward completion. If externally blocked, finish independent work and report PARTIAL/BLOCKED with exact missing input and evidence. Do not hide blockers behind a score. Do not push/deploy publicly, publish on Amazon, buy credits or modify unrelated shared resources.

Final handoff must give: runnable URL and start/stop commands, exact revision, score and all hard gates, real demo book/artifact paths, observed limits, provider/Telegram setup state, backup/recovery steps, unresolved findings, critic confidence/reason and unique agent count from the append-only ledger.
