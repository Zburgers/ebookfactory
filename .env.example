# Local peer-authenticated PostgreSQL target provisioned by the owner.
EBOOK_FACTORY_ENV=local
EBOOK_FACTORY_DATABASE_URL=postgresql+psycopg://naki@/ebookfactory?host=/var/run/postgresql
EBOOK_FACTORY_ARTIFACT_ROOT=./var/artifacts
# Required only for private supervisor callbacks; keep it local and untracked.
EBOOK_FACTORY_WORKER_TOKEN=
# Required for dashboard/API owner routes; never reuse the worker token.
EBOOK_FACTORY_OWNER_TOKEN=
# Optional trusted certificate/key for non-loopback HTTPS. If omitted, the
# launcher creates a private self-signed certificate under var/tls.
EBOOK_FACTORY_TLS_CERT=
EBOOK_FACTORY_TLS_KEY=
# Telegram remains visibly unconfigured until the owner supplies all three local values.
EBOOK_FACTORY_TELEGRAM_BOT_TOKEN=
EBOOK_FACTORY_TELEGRAM_ALLOWED_CHAT_IDS=
EBOOK_FACTORY_TELEGRAM_ALLOWED_SENDER_IDS=
