# Evidence scaffold

Per-packet evidence contains code revision, UTC, runtime versions, scoped target identity, action/command, expected and observed results, artifact hashes and concise screenshots/log excerpts. Synthetic book content preferred. No API keys, OAuth tokens, bot tokens, auth files or raw environment dumps. Final delivery evidence is compiled in final/ by P10, not prefilled as success.
